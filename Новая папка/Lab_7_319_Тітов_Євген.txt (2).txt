       
    Лабораторна робота №7 номер роботи
    
    з дисципліни «Алгоритмізація та програмування»
    
    
   Тема: "Реалізація алгоритмів обробки двовимірних масивів на мові С ++"    
    	 
                             Виконав студент гр. 319 Тітов Євген
                          		       

		МЕТА РОБОТИ
  Вивчити теоретичний матеріал з основ представлення двовимірних масивів
(матриць) на мові С ++ і реалізувати оголошення, введення з консолі, обробку і
виведення в консоль матриць на мові C ++ в середовищі Visual Studio.    

    
	  ПОСТАНОВКА ЗАДАЧІ
    
Завдання 1. Дана матриця розміру M ? N. У кожному її рядку визначити кількість
елементів, менших ніж середнє арифметичне всіх елементів цього рядка. Matrix29

Завдання 2. Дана матриця розміру M ? N. Перед першим стовпцем, що містить тільки додатні елементи, вставити стовпець з одиниць. 
Якщо необхідних стовпців немає, то вивести матрицю без змін. Matrix72.   

		ВИКОНАННЯ РОБОТИ
    
    Завдання 1. 
    Matrix29 таб.1
    Вхідні дані (ім’я, опис, тип, обмеження): матриця МхN тип const
    список імен змінних з описом, типами даних, та діапазонами допустимих значень
    Вихідні дані (ім’я, опис, тип): b,c,d,e,f,g
   

    Лістинг коду вирішення задачі 

код зі збереженням відступів, коментарями
Matrix29

#include <stdio.h> //підключення бібліотеки
 
const M = 100; //визначння змінних
const N = 100; //визначння змінних
var a : array[1..M, 1..N] of integer;
  b, c, d, e, f, g : integer;
begin
  randomize;
  e := 0;
  writeln('введіть кількість строк та столбців: '); // вивести на екран
  readln(d, g); // зчитати з екрану 
  for b:=1 to d do begin // умова
    for c:=1 to g do begin // умова
      a[b,c]:= random(100);
      end;
  end;
  f:=0;
  for b:=1 to d do begin // умова
    for c:=1 to g do // умова
      e:=e + a[b,c];
    for c:=1 to d do // умова
      if a[b,c] < e / d then // умова
        f := f + 1;
    writeln('строка №', b, ':', f); // вивести на екран
    e:=0;
    f := 0;
  end;
end.


    Завдання 2. 
    Matrix72 таб.2
    Вхідні дані (ім’я, опис, тип, обмеження): матриця МхN тип int
    список імен змінних з описом, типами даних, та діапазонами допустимих значень
    Вихідні дані (ім’я, опис, тип): M, N тип int


    Лістинг коду вирішення задачі 

код зі збереженням відступів, коментарями   
Matrix72.   

#include <iostream> //підключення бібліотеки
#include <iomanip> //підключення бібліотеки
#include <ctime> //підключення бібліотеки
using namespace std;
 
int main()
{
    srand((int)time(0));
    int M, N, jk=0, k, p=0;
    cout <<"Enter M: "; cin >>M;
    cout <<"Enter N: "; cin >>N;
    
      double **a = new double*[M]; 
    for (int i = 0; i < M; i++) // умова
      a[i]=new double[N];    
    
    for (int i = 0; i < M; i++) // умова
    {
    for (int j = 0; j < N; j++) // умова
    {
    a[i][j]=rand()%12-2;
    cout <<setw(3)<<a[i][j]<<" "; // відобразити
    }
    cout << endl; // відобразити
    }
    cout << endl; // відобразити
    
    for (int j = 0; j < N; j++) // умова
    {
    k=0;
    for (int i = 0; i < M; i++) // умова
    if (a[i][j]>0) k++; // умова
    if (k==M) {jk=j; p++; break;} // умова 
    }    
    
    if (p!=0) // умова
    {
      double **b = new double*[M]; 
    for (int i = 0; i < M; i++) // умова     
      b[i]=new double[N+1];
      
    for (int i = 0; i < M; i++) // умова    
    for (int j = 0; j < N; j++) // умова    
    b[i][j]=a[i][j]; 
    
    for (int i = 0; i < M; i++) // умова
    delete [] a[i]; // видалити
    delete [] a; // видалити
      
    for (int j = N-1; j >jk-1; j--) // умова  
    for (int i = 0; i < M; i++) // умова
    b[i][j+1]=b[i][j];
    
    cout <<"Enter "<<M<<"  '1':\n"; // відобразити
    for (int i = 0; i < M; i++) // умова
    cin >>b[i][jk];
    
    a=b;
    
    for (int i = 0; i < M; i++) // умова
    {
    for (int j = 0; j < N+1; j++) // умова    
    cout <<setw(3)<<a[i][j]<<" "; // відобразити    
    cout << endl; // відобразити
    }
    for (int i = 0; i < M+1; i++) // умова
    delete [] b[i]; // видалити
    delete [] b; // видалити
    }
    else // умова
    {
    for (int i = 0; i < M; i++) // умова
    {
    for (int j = 0; j < N; j++) // умова    
    cout <<setw(3)<<a[i][j]<<" "; // відобразити    
    cout << endl;
    }    
    for (int i = 0; i < M; i++) // умова
    delete [] a[i]; // видалити
    delete [] a; // видалити 
    }
system("pause");
return 0;
}

    
     
		ВИСНОВКИ
     
Було вивчено теоретичний матеріал з основ представлення двовимірних і
масивів на мові С ++ і реалізувано оголошення, введення з консолі, обробку і
виведення в консоль одновимірних масивів на мові C ++ в середовищі Visual
Studio.
Закріплено на практиці тему: "реалізація алгоритмів обробки двовимірних масивів на мові С ++"
Відпрацьовано в коді програми введення з консолі, обробку і виведення в консоль двовимірних масивів на мові C ++
Отримано навички з основ представлення двовимірних і масивів на мові С ++
     


   

